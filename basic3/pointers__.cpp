/* Date: 20 - 07 - 2022
Author: Sailendra */

#include <iostream>
#include <bits/stdc++.h>
using namespace std;

int main()
{
    freopen("input.txt", "r", stdin);
    freopen("output.txt", "w", stdout);
/*
    int num = 10;
    // cout<<"value of num: "<<num<<endl;
    // cout<<"address of num: "<<&num<<endl;

    int *ptr = &num; // ptr is the pointer to integer
    // cout<<"dereferencing pointer: "<<*ptr<<endl;
    // cout<<"value of pointer: "<<ptr<<endl;
    // cout<<"address of pointer: "<<&ptr<<endl;

    int a = 10;
    int *p = &a;

    char b = 'b';
    char *p1 = &b;

    double c = 9.12;
    double *p2 = &c;

    bool d = true;
    bool *p3 = &d;

    float e = true;
    float *p4 = &e;
    cout<<"size of datatype int type: "<<sizeof(*p)<<endl;
    cout<<"size of datatype char type: "<<sizeof(*p1)<<endl;
    cout<<"size of datatype double type: "<<sizeof(*p2)<<endl;
    cout<<"size of datatype bool type: "<<sizeof(*p3)<<endl;
    cout<<"size of pointer float type: "<<sizeof(p4)<<endl<<endl;
    cout<<"size of pointer int type: "<<sizeof(p)<<endl;
    cout<<"size of pointer char type: "<<sizeof(p1)<<endl;
    cout<<"size of pointer idoublent type: "<<sizeof(p2)<<endl;
    cout<<"size of pointer bool type: "<<sizeof(p3)<<endl;
    cout<<"size of pointer float type: "<<sizeof(p4)<<endl;
*/


    // int *p; // don't do this - BAD PRACTICE
    // cout<<*p<<endl;
    // cout<<p<endl;

/*

    int num = 4;
    int *ptr = &num;
    cout<<"num: "<<num<<endl;
    cout<<"*ptr: "<<*ptr<<endl;

    (*ptr) = (*ptr) * 2;

    cout<<"num: "<<num<<endl;
    cout<<"*ptr: "<<*ptr<<endl;

*/

    // copying a pointer
    // int num = 9;
    // int *ptr = &num;
    // cout<<"num: "<<num<<endl;
    // cout<<"ptr: "<<ptr<<endl;
    // cout<<"*ptr: "<<*ptr<<endl;

    // int *copyPtr = ptr;
    // cout<<"copyPtr: "<<copyPtr<<endl;
    // cout<<"*copyPtr: "<<*copyPtr<<endl;

    /*
    // ---------------INCREMENT THE ADDRESS OF POINTER---------------
    cout<<"before -> ptr: "<<ptr<<endl;
    ptr = ptr + 2;
    cout<<"after -> ptr: "<<ptr;
    */












    return 0;
}